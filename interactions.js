(() => {
  const style = document.createElement('style');
  style.textContent = `.teacher-note,.coach-box,.teacher-check{display:none!important}.reveal-wrap{margin:1.15rem 0}.reveal-btn{display:inline-flex;align-items:center;justify-content:center;gap:.45rem;min-height:48px;border:2px solid var(--ink);background:var(--yellow);color:var(--ink);padding:.7rem 1rem;font:700 .76rem 'DM Sans';letter-spacing:.06em;cursor:pointer;box-shadow:4px 4px 0 var(--ink);transition:transform .2s,box-shadow .2s}.reveal-btn:hover{transform:translate(-2px,-2px);box-shadow:6px 6px 0 var(--ink)}.reveal-btn.open{background:var(--lilac)}.reveal-panel{display:none;background:#ede3f7;border:2px solid var(--ink);padding:1rem 1.1rem;margin-top:.75rem;line-height:1.55;font-size:.95rem;box-shadow:5px 5px 0 var(--lilac)}.reveal-panel.show{display:block;animation:slideIn .25s ease}.reveal-panel b{display:inline-block;background:var(--ink);color:var(--cream);font-size:.68rem;letter-spacing:.08em;text-transform:uppercase;margin-bottom:.65rem;padding:.35rem .55rem}.student-action{display:flex;align-items:center;gap:.55rem;flex-wrap:wrap;margin-top:.9rem}.timer-btn{min-height:44px;border:1px solid var(--orange);background:var(--cream);padding:.55rem .75rem;font:700 .72rem 'DM Sans';cursor:pointer}.timer-btn.active{background:var(--orange);color:#fff}.timer-display{font:700 1.15rem 'Bebas Neue';letter-spacing:.08em;color:var(--orange);min-width:3.5rem}.student-tip{color:var(--muted);font-size:.78rem;line-height:1.45;border-top:1px solid var(--line);padding-top:1rem;margin-top:1.4rem}@keyframes slideIn{from{opacity:0;transform:translateY(-5px) scale(.98)}to{opacity:1;transform:none}}`;
  document.head.appendChild(style);
  const card = document.getElementById('lessonCard'); let timerId;
  function enhance(){if(!card)return;card.querySelectorAll('.deep-dive').forEach(item=>{if(item.dataset.enhanced)return;item.dataset.enhanced='true';const raw=item.innerHTML,match=raw.match(/<b>(.*?)<\/b>(.*)/s),content=match?match[2].trim():raw,wrap=document.createElement('div');wrap.className='reveal-wrap';wrap.innerHTML=`<button class="reveal-btn" type="button">👆 НАЖМИ МЕНЯ — СЛЕДУЮЩИЙ ВОПРОС!</button><div class="reveal-panel"><b>✨ ДОПОЛНИТЕЛЬНЫЙ ВОПРОС</b><div>${content}</div><div class="student-action"><button class="timer-btn" type="button">⏱ Ответить за 30 секунд</button><span class="timer-display"></span></div></div>`;item.replaceWith(wrap);const reveal=wrap.querySelector('.reveal-btn'),panel=wrap.querySelector('.reveal-panel');reveal.onclick=()=>{panel.classList.toggle('show');reveal.classList.toggle('open');reveal.textContent=panel.classList.contains('show')?'✅ ВОПРОС ОТКРЫТ':'👆 НАЖМИ МЕНЯ — СЛЕДУЮЩИЙ ВОПРОС!'};const timer=wrap.querySelector('.timer-btn'),display=wrap.querySelector('.timer-display');timer.onclick=()=>{clearInterval(timerId);let seconds=30;timer.classList.add('active');timer.textContent='⏸ Идёт время…';display.textContent='00:30';timerId=setInterval(()=>{seconds--;display.textContent=`00:${String(seconds).padStart(2,'0')}`;if(seconds<=0){clearInterval(timerId);timer.classList.remove('active');timer.textContent='↻ Попробовать ещё раз';display.textContent='ГОТОВО!'}},1000)} });card.querySelectorAll('.scene-option').forEach(button=>{const row=button.parentElement;if(row&&!row.dataset.freeAnswer){row.dataset.freeAnswer='true';row.innerHTML='<input class="answer-input" placeholder="The telescope is… / The rocket is…" aria-label="Describe the location" />'}});if(!card.querySelector('.student-tip')){const tip=document.createElement('p');tip.className='student-tip';tip.textContent='Ответь на главный вопрос, а затем нажми кнопку — там тебя ждёт следующий уровень!';card.appendChild(tip)}}
  new MutationObserver(enhance).observe(card,{childList:true,subtree:true}); enhance();
})();
